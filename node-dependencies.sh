#!/bin/bash

# Package Dependencies:
#   net
#
# Must have nodejs installed, if not, run the following lines first
#   $ sudo apt install nodejs
#   $ sudo apt install npm

npm init
npm i -g npm
npm i net